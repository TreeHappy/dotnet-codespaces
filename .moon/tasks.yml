tasks:
  # format:
  #   command: 'prettier --check .'
  # lint:
  #   command: 'eslint --no-error-on-unmatched-pattern .'
  test:
    command: 'dotnet test'
  # typecheck:
  #   command: 'tsc --build'
